# failing exec example
#
# demonstrates a command that we expect to fail to start.

actor never_runs = exec,
  command: "python4 -m http.server 8080"

# note: if you actually have a python4 installed, that will break this test
as never_runs:
  run
  assert error.exec.reason == :command_not_found
