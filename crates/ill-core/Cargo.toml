[package]
name = "ill-core"
description = "Core library for iLL — AST types and tree-sitter CST lowering"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
thiserror = "2"
tree-sitter = "0.26"
tree-sitter-ill = { path = "../../tree-sitter-ill" }

[dev-dependencies]
glob = "0.3"
